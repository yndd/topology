apiVersion: topo.nddr.yndd.io/v1alpha1
kind: TopologyLink
metadata:
  name: link48-leaf2-dcgw2
  namespace: default
spec:
  oda:
  - key: organization
    value: nokia
  - key: deployment
    value: region1
  topology-name: fabric1
  link:
    tag:
    endpoints:
    - node-name: leaf2
      interface-name: int-1/1/48
      tag:
      - {key: kind, value: access}
      - {key: endpoint-group, value: dcgw}
    - node-name: dcgw2
      interface-name: int-1/1/1
      tag:
      - {key: kind, value: access}
